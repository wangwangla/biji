# 计算机概论
```

```
## 计算机硬件的组成
```
    输入 输出设备
    中央处理设备：算术逻辑单元和控制单元
    内存

    输入输出数据先进入内存，然后被CPU执行，外部存储设备的数据，如果要操作也会先进入内存，然后在进入CPU进行处理，数据进入内存，又是CPU的指令控制的 。

```
## 接口设备
```
    单个输入输出、cpu的连接使用主板，它将所以设备连接起来，能让其可以协调的通信
    
    存储设备、显示设备、网络设备

```
## 常用的计算单位
```
    0/1单位的bit  8bit一个字节  1024bit是1k 1024k是1M  1024M是1G

    1T的硬盘为什么回来不是那么大？
        1000X1000X1000X1000B  化成内存的进制1024就是小于1T.

```
## 个人计算机架构与接口设备
```
    主板最重要的部分是芯片组
    芯片组有分为两个桥接器来控制各组件进行通信
        （1）北桥接负责连接速度较快的CPU、内存与显卡
        （2）南桥负责速度较慢的周边接口 硬盘、USB、网卡。

        AMD芯片组内存直接于CPU不需要北桥[为了效率]。

```
## CPU
```

    1.x86计算机的两大主流AMD和Intel
    2.CPU运行会发大量的热。
    3.CPU的多核一个壳子里面有多个运算器
    4.不同的COU具有不同的脚，搭配的主板也是不一样的。
    5.CPU里面不同的指令集会有不同的效率。
      频率：就是CPU每秒可以工作的次数。
        注意：不能以频率来判断速度，因为指令集是不一样的。
    6.CPU的倍频和外频
      CPU可以高速的运算，但是外部设备速度很慢，为了可以更好的工作，所以CPU有外频
      倍频是CPU的频率比外频取整数。

      CPU的频率和外频，设置超频的时候，因为频率是锁死的，只能加快外频，这也会造成死机。
    7.速度主要是受外频控制，但是传递的数据量这个需要通过总结来实现了，一般主板分为南北桥，北桥系统总线是内存的主要信道，速度快。南桥输入输出设备的总线，速度慢。

```
## 内存
```


